{
  "name": "@orca-so/whirlpools-rust-macros",
  "version": "1.0.3",
  "scripts": {
    "build": "cargo build",
    "test": "cargo test --lib",
    "publish": "sh ../../scripts/publish-cargo",
    "format": "cargo clippy --fix --allow-dirty --allow-staged && cargo fmt",
    "lint": "cargo clippy && cargo fmt --check",
    "clean": "cargo clean"
  }
}
