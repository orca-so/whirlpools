/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { WHIRLPOOL_PROGRAM_ADDRESS } from "../programs";
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const INITIALIZE_PREPARED_SWAP_DISCRIMINATOR = new Uint8Array([
  87, 29, 144, 47, 96, 8, 34, 45,
]);

export function getInitializePreparedSwapDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_PREPARED_SWAP_DISCRIMINATOR,
  );
}

export type InitializePreparedSwapInstruction<
  TProgram extends string = typeof WHIRLPOOL_PROGRAM_ADDRESS,
  TAccountFunder extends string | AccountMeta<string> = string,
  TAccountPreparedSwap extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountFunder extends string
        ? WritableSignerAccount<TAccountFunder> &
            AccountSignerMeta<TAccountFunder>
        : TAccountFunder,
      TAccountPreparedSwap extends string
        ? WritableAccount<TAccountPreparedSwap>
        : TAccountPreparedSwap,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializePreparedSwapInstructionData = {
  discriminator: ReadonlyUint8Array;
  nonce: number;
};

export type InitializePreparedSwapInstructionDataArgs = { nonce: number };

export function getInitializePreparedSwapInstructionDataEncoder(): FixedSizeEncoder<InitializePreparedSwapInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["nonce", getU16Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_PREPARED_SWAP_DISCRIMINATOR,
    }),
  );
}

export function getInitializePreparedSwapInstructionDataDecoder(): FixedSizeDecoder<InitializePreparedSwapInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["nonce", getU16Decoder()],
  ]);
}

export function getInitializePreparedSwapInstructionDataCodec(): FixedSizeCodec<
  InitializePreparedSwapInstructionDataArgs,
  InitializePreparedSwapInstructionData
> {
  return combineCodec(
    getInitializePreparedSwapInstructionDataEncoder(),
    getInitializePreparedSwapInstructionDataDecoder(),
  );
}

export type InitializePreparedSwapAsyncInput<
  TAccountFunder extends string = string,
  TAccountPreparedSwap extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  funder: TransactionSigner<TAccountFunder>;
  preparedSwap?: Address<TAccountPreparedSwap>;
  systemProgram?: Address<TAccountSystemProgram>;
  nonce: InitializePreparedSwapInstructionDataArgs["nonce"];
};

export async function getInitializePreparedSwapInstructionAsync<
  TAccountFunder extends string,
  TAccountPreparedSwap extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof WHIRLPOOL_PROGRAM_ADDRESS,
>(
  input: InitializePreparedSwapAsyncInput<
    TAccountFunder,
    TAccountPreparedSwap,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializePreparedSwapInstruction<
    TProgramAddress,
    TAccountFunder,
    TAccountPreparedSwap,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? WHIRLPOOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    funder: { value: input.funder ?? null, isWritable: true },
    preparedSwap: { value: input.preparedSwap ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.preparedSwap.value) {
    accounts.preparedSwap.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            112, 114, 101, 112, 97, 114, 101, 100, 95, 115, 119, 97, 112,
          ]),
        ),
        getU16Encoder().encode(expectSome(args.nonce)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.funder),
      getAccountMeta(accounts.preparedSwap),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializePreparedSwapInstructionDataEncoder().encode(
      args as InitializePreparedSwapInstructionDataArgs,
    ),
    programAddress,
  } as InitializePreparedSwapInstruction<
    TProgramAddress,
    TAccountFunder,
    TAccountPreparedSwap,
    TAccountSystemProgram
  >);
}

export type InitializePreparedSwapInput<
  TAccountFunder extends string = string,
  TAccountPreparedSwap extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  funder: TransactionSigner<TAccountFunder>;
  preparedSwap: Address<TAccountPreparedSwap>;
  systemProgram?: Address<TAccountSystemProgram>;
  nonce: InitializePreparedSwapInstructionDataArgs["nonce"];
};

export function getInitializePreparedSwapInstruction<
  TAccountFunder extends string,
  TAccountPreparedSwap extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof WHIRLPOOL_PROGRAM_ADDRESS,
>(
  input: InitializePreparedSwapInput<
    TAccountFunder,
    TAccountPreparedSwap,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializePreparedSwapInstruction<
  TProgramAddress,
  TAccountFunder,
  TAccountPreparedSwap,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? WHIRLPOOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    funder: { value: input.funder ?? null, isWritable: true },
    preparedSwap: { value: input.preparedSwap ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.funder),
      getAccountMeta(accounts.preparedSwap),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializePreparedSwapInstructionDataEncoder().encode(
      args as InitializePreparedSwapInstructionDataArgs,
    ),
    programAddress,
  } as InitializePreparedSwapInstruction<
    TProgramAddress,
    TAccountFunder,
    TAccountPreparedSwap,
    TAccountSystemProgram
  >);
}

export type ParsedInitializePreparedSwapInstruction<
  TProgram extends string = typeof WHIRLPOOL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    funder: TAccountMetas[0];
    preparedSwap: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializePreparedSwapInstructionData;
};

export function parseInitializePreparedSwapInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializePreparedSwapInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      funder: getNextAccount(),
      preparedSwap: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializePreparedSwapInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
